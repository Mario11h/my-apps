// src/Components/OverviewGrid/OverviewGridStyles.ts

import { styled } from '@mui/material/styles';
import { Typography } from '@mui/material';

export const OverviewText = styled(Typography)({
    color: 'rgba(0, 0, 0, 0.65)', // Black lighter 35%
    fontFamily: 'roboto',
    fontSize: '14px',
});
